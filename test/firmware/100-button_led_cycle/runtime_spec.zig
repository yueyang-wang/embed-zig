pub const timing = struct {
    pub const long_press_ms: u64 = 1000;
    pub const double_click_window_ms: u64 = 300;
};

pub const devices = struct {
    pub const button = "btn_boot";
    pub const led = "led0";
};
